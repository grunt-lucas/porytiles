#pragma once

#include <algorithm>
#include <cctype>
#include <format>
#include <optional>
#include <ostream>
#include <string>

#include "porytiles/utilities/panic/panic.hpp"

/*
 * NOTE: DO NOT EDIT THIS FILE DIRECTLY. It is AUTO-GENERATED from config_schema.yaml.
 * To add new config values or make other changes, edit config_schema.yaml and regenerate via:
 *
 *   uv run scripts/generate_config.py
 */

namespace porytiles {

/**
 * @brief Controls whether palette packing considers tile sharing shape group membership.
 *
 * @details
 * When set to off, the packer ignores shape groups entirely. When biased,
 * a soft cost penalty steers shape group siblings toward different palettes. When
 * optimal, co-placing siblings is rejected outright (not yet implemented).
 *
 */
enum class TileSharingPacking {
    /**
     * @brief Packing ignores shape group membership (default).
     */
    off,
    /**
     * @brief Soft penalty steers shape group siblings toward different palettes.
     */
    biased,
    /**
     * @brief Hard constraint rejecting sibling co-placement (not yet implemented).
     */
    optimal
};

/**
 * @brief Parses a string into a TileSharingPacking with fuzzy matching.
 *
 * @details
 * Performs parsing in two phases:
 * 1. Exact match against C++ constant names (snake_case)
 * 2. Case-insensitive fuzzy match against configured fuzzy_names
 *
 * This unified function is used by both CLI and YAML parsing. The fuzzy matching
 * allows users to use various common formats (kebab-case, snake_case, etc.) while
 * still getting good error messages through LazyLayeredConfig.
 *
 * @param str The string to parse
 * @return The parsed value, or std::nullopt if the string is invalid
 */
[[nodiscard]] inline std::optional<TileSharingPacking> tile_sharing_packing_from_str(const std::string &str)
{
    // Phase 1: Exact match against C++ constant names
    if (str == "off") {
        return std::optional{TileSharingPacking::off};
    }
    if (str == "biased") {
        return std::optional{TileSharingPacking::biased};
    }
    if (str == "optimal") {
        return std::optional{TileSharingPacking::optimal};
    }

    // Phase 2: Case-insensitive fuzzy match
    std::string lower_str = str;
    std::ranges::transform(
        lower_str, lower_str.begin(), [](unsigned char c) { return static_cast<char>(std::tolower(c)); });

    // Fuzzy names for off
    if (lower_str == "off") {
        return std::optional{TileSharingPacking::off};
    }
    // Fuzzy names for biased
    if (lower_str == "biased") {
        return std::optional{TileSharingPacking::biased};
    }
    // Fuzzy names for optimal
    if (lower_str == "optimal") {
        return std::optional{TileSharingPacking::optimal};
    }

    return std::nullopt;
}

/**
 * @brief Converts a TileSharingPacking to its canonical string representation.
 *
 * @details
 * Returns the C++ constant name (snake_case). This is the canonical form used for
 * general purpose string conversion, logging, and error messages.
 *
 * @param m The value to convert
 * @return The canonical snake_case string representation
 */
[[nodiscard]] inline std::string to_string(const TileSharingPacking m)
{
    switch (m) {
    case TileSharingPacking::off:
        return "off";
    case TileSharingPacking::biased:
        return "biased";
    case TileSharingPacking::optimal:
        return "optimal";
    }
    panic("unhandled TileSharingPacking value");
}

/**
 * @brief Stream insertion operator for TileSharingPacking.
 *
 * @param os The output stream
 * @param m The value to output
 * @return Reference to the output stream
 */
inline std::ostream &operator<<(std::ostream &os, const TileSharingPacking m)
{
    return os << to_string(m);
}

} // namespace porytiles

template <>
struct std::formatter<porytiles::TileSharingPacking> {
    constexpr auto parse(std::format_parse_context &ctx)
    {
        return ctx.begin();
    }

    auto format(const porytiles::TileSharingPacking &value, auto &ctx) const
    {
        return std::format_to(ctx.out(), "{}", porytiles::to_string(value));
    }
};
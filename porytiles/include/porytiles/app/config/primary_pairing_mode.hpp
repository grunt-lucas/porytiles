#pragma once

#include <algorithm>
#include <cctype>
#include <format>
#include <optional>
#include <ostream>
#include <string>

#include "porytiles/utilities/panic/panic.hpp"

/*
 * NOTE: DO NOT EDIT THIS FILE DIRECTLY. It is AUTO-GENERATED from config_schema.yaml.
 * To add new config values or make other changes, edit config_schema.yaml and regenerate via:
 *
 *   uv run scripts/generate_config.py
 */

namespace porytiles {

/**
 * @brief Controls how a secondary tileset finds its partner primary for compilation.
 *
 * @details
 * When compiling a secondary tileset, the compiler needs the compiled primary
 * tileset to produce correct global tile indices and palette references.
 * This mode controls how the partner primary is resolved.
 *
 */
enum class PrimaryPairingMode {
    /**
     * @brief Skip primary loading entirely. Compile as a standalone secondary.
     */
    off,
    /**
     * @brief Use the partner primary specified via primary_pairing_partners config.
     */
    manual,
    /**
     * @brief Scan layout metadata to find the partner primary automatically.
     */
    automatic
};

/**
 * @brief Parses a string into a PrimaryPairingMode with fuzzy matching.
 *
 * @details
 * Performs parsing in two phases:
 * 1. Exact match against C++ constant names (snake_case)
 * 2. Case-insensitive fuzzy match against configured fuzzy_names
 *
 * This unified function is used by both CLI and YAML parsing. The fuzzy matching
 * allows users to use various common formats (kebab-case, snake_case, etc.) while
 * still getting good error messages through LazyLayeredConfig.
 *
 * @param str The string to parse
 * @return The parsed value, or std::nullopt if the string is invalid
 */
[[nodiscard]] inline std::optional<PrimaryPairingMode> primary_pairing_mode_from_str(const std::string &str)
{
    // Phase 1: Exact match against C++ constant names
    if (str == "off") {
        return std::optional{PrimaryPairingMode::off};
    }
    if (str == "manual") {
        return std::optional{PrimaryPairingMode::manual};
    }
    if (str == "automatic") {
        return std::optional{PrimaryPairingMode::automatic};
    }

    // Phase 2: Case-insensitive fuzzy match
    std::string lower_str = str;
    std::ranges::transform(
        lower_str, lower_str.begin(), [](unsigned char c) { return static_cast<char>(std::tolower(c)); });

    // Fuzzy names for off
    if (lower_str == "off") {
        return std::optional{PrimaryPairingMode::off};
    }
    if (lower_str == "none") {
        return std::optional{PrimaryPairingMode::off};
    }
    if (lower_str == "disabled") {
        return std::optional{PrimaryPairingMode::off};
    }
    // Fuzzy names for manual
    if (lower_str == "manual") {
        return std::optional{PrimaryPairingMode::manual};
    }
    // Fuzzy names for automatic
    if (lower_str == "automatic") {
        return std::optional{PrimaryPairingMode::automatic};
    }
    if (lower_str == "auto") {
        return std::optional{PrimaryPairingMode::automatic};
    }

    return std::nullopt;
}

/**
 * @brief Converts a PrimaryPairingMode to its canonical string representation.
 *
 * @details
 * Returns the C++ constant name (snake_case). This is the canonical form used for
 * general purpose string conversion, logging, and error messages.
 *
 * @param m The value to convert
 * @return The canonical snake_case string representation
 */
[[nodiscard]] inline std::string to_string(const PrimaryPairingMode m)
{
    switch (m) {
    case PrimaryPairingMode::off:
        return "off";
    case PrimaryPairingMode::manual:
        return "manual";
    case PrimaryPairingMode::automatic:
        return "automatic";
    }
    panic("unhandled PrimaryPairingMode value");
}

/**
 * @brief Stream insertion operator for PrimaryPairingMode.
 *
 * @param os The output stream
 * @param m The value to output
 * @return Reference to the output stream
 */
inline std::ostream &operator<<(std::ostream &os, const PrimaryPairingMode m)
{
    return os << to_string(m);
}

} // namespace porytiles

template <>
struct std::formatter<porytiles::PrimaryPairingMode> {
    constexpr auto parse(std::format_parse_context &ctx)
    {
        return ctx.begin();
    }

    auto format(const porytiles::PrimaryPairingMode &value, auto &ctx) const
    {
        return std::format_to(ctx.out(), "{}", porytiles::to_string(value));
    }
};
#pragma once

#include <algorithm>
#include <cctype>
#include <format>
#include <optional>
#include <ostream>
#include <string>

#include "porytiles/utilities/panic/panic.hpp"

// NOTE: DO NOT EDIT THIS FILE DIRECTLY. It is AUTO-GENERATED from config_schema.yaml.
// To add new config values or make other changes, edit config_schema.yaml and regenerate via:
//
//   uv run scripts/generate_config.py

namespace porytiles {

/// @brief Controls whether the FireRed/LeafGreen alternate metatile attribute masks are selected for a tileset.
///
/// @details
/// FireRed and LeafGreen pack metatile attributes differently than Ruby/Sapphire/Emerald: the fields
/// use different bit masks and the layout is typically four bytes wide instead of two. This mode controls
/// whether Porytiles selects those alternate masks when resolving a tileset's attribute schema. In
/// automatic mode, Porytiles cross-references data/layouts/layouts.json and reads each layout's
/// layout_version to decide per tileset. The always and never values force an explicit choice, which is useful
/// when a tileset has no layouts.json entry or is referenced by both emerald and frlg layouts. This key
/// is intended to be set at the tileset scope.
///
enum class FrlgAlternateMaskMode {
    /// @brief Cross-reference layouts.json layout_version to decide the masks per tileset.
    automatic,
    /// @brief Always select the FRLG alternate masks for this tileset.
    always,
    /// @brief Never select the FRLG alternate masks; use the primary masks.
    never
};

/// @brief Parses a string into a FrlgAlternateMaskMode with fuzzy matching.
///
/// @details
/// Performs parsing in two phases:
/// 1. Exact match against C++ constant names (snake_case)
/// 2. Case-insensitive fuzzy match against configured fuzzy_names
///
/// This unified function is used by both CLI and YAML parsing. The fuzzy matching
/// allows users to use various common formats (kebab-case, snake_case, etc.) while
/// still getting good error messages through LazyLayeredConfig.
///
/// @param str The string to parse
/// @return The parsed value, or std::nullopt if the string is invalid
[[nodiscard]] inline std::optional<FrlgAlternateMaskMode> frlg_alternate_mask_mode_from_str(const std::string &str)
{
    // Phase 1: Exact match against C++ constant names
    if (str == "automatic") {
        return std::optional{FrlgAlternateMaskMode::automatic};
    }
    if (str == "always") {
        return std::optional{FrlgAlternateMaskMode::always};
    }
    if (str == "never") {
        return std::optional{FrlgAlternateMaskMode::never};
    }

    // Phase 2: Case-insensitive fuzzy match
    std::string lower_str = str;
    std::ranges::transform(
        lower_str, lower_str.begin(), [](unsigned char c) { return static_cast<char>(std::tolower(c)); });

    // Fuzzy names for automatic
    if (lower_str == "automatic") {
        return std::optional{FrlgAlternateMaskMode::automatic};
    }
    if (lower_str == "auto") {
        return std::optional{FrlgAlternateMaskMode::automatic};
    }
    // Fuzzy names for always
    if (lower_str == "always") {
        return std::optional{FrlgAlternateMaskMode::always};
    }
    if (lower_str == "true") {
        return std::optional{FrlgAlternateMaskMode::always};
    }
    if (lower_str == "yes") {
        return std::optional{FrlgAlternateMaskMode::always};
    }
    if (lower_str == "on") {
        return std::optional{FrlgAlternateMaskMode::always};
    }
    // Fuzzy names for never
    if (lower_str == "never") {
        return std::optional{FrlgAlternateMaskMode::never};
    }
    if (lower_str == "false") {
        return std::optional{FrlgAlternateMaskMode::never};
    }
    if (lower_str == "no") {
        return std::optional{FrlgAlternateMaskMode::never};
    }
    if (lower_str == "off") {
        return std::optional{FrlgAlternateMaskMode::never};
    }

    return std::nullopt;
}

/// @brief Converts a FrlgAlternateMaskMode to its canonical string representation.
///
/// @details
/// Returns the C++ constant name (snake_case). This is the canonical form used for
/// general purpose string conversion, logging, and error messages.
///
/// @param m The value to convert
/// @return The canonical snake_case string representation
[[nodiscard]] inline std::string to_string(const FrlgAlternateMaskMode m)
{
    switch (m) {
    case FrlgAlternateMaskMode::automatic:
        return "automatic";
    case FrlgAlternateMaskMode::always:
        return "always";
    case FrlgAlternateMaskMode::never:
        return "never";
    }
    panic("unhandled FrlgAlternateMaskMode value");
}

/// @brief Stream insertion operator for FrlgAlternateMaskMode.
///
/// @param os The output stream
/// @param m The value to output
/// @return Reference to the output stream
inline std::ostream &operator<<(std::ostream &os, const FrlgAlternateMaskMode m)
{
    return os << to_string(m);
}

} // namespace porytiles

template <>
struct std::formatter<porytiles::FrlgAlternateMaskMode> {
    constexpr auto parse(std::format_parse_context &ctx)
    {
        return ctx.begin();
    }

    auto format(const porytiles::FrlgAlternateMaskMode &value, auto &ctx) const
    {
        return std::format_to(ctx.out(), "{}", porytiles::to_string(value));
    }
};
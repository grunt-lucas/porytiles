{#
  Template for generating cli_option_registration.hpp

  This template generates the function declaration for registering CLI options with CLI11.
#}
{% import '_macros.jinja2' as macros %}
#pragma once

#include "CLI/CLI.hpp"

#include "porytiles2/infra/cli/cli_option_storage.hpp"

namespace porytiles2 {

{{ macros.autogen_warning() }}

/**
 * @brief Registers all config options with a CLI11 App.
 *
 * @details
 * This function adds all config values as CLI options to the provided CLI::App. The options
 * are grouped under "Config Options" and will populate the corresponding fields in the
 * CliOptionStorage struct when parsed.
 *
 * @param app The CLI11 App to register options with
 * @param storage The storage struct that will hold parsed values
 */
void register_config_options(CLI::App &app, CliOptionStorage &storage);

} // namespace porytiles2

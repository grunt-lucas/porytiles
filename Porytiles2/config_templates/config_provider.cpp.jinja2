{#
  Template for generating config_provider.cpp

  This template generates the default implementations for ConfigProvider methods
  that return not_provided() for all configuration values.
#}
{% import '_macros.jinja2' as macros %}
#include "porytiles2/infra/config/config_provider.hpp"

{{ macros.autogen_warning() }}

namespace porytiles2 {
{% for value in config_values %}

LayerValue<{{ value.type }}> ConfigProvider::{{ value.name }}([[maybe_unused]] ConfigScopeType type, [[maybe_unused]] const std::string &scope) const
{
    return LayerValue<{{ value.type }}>::not_provided();
}
{% endfor %}

} // namespace porytiles2

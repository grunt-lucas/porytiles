{% import '_macros.jinja2' as macros %}
#pragma once

{{ macros.autogen_warning() }}

#include <string>

#include "porytiles2/infra/config/infra_config.hpp"

namespace porytiles2 {

class MockInfraConfig : public InfraConfig {
  public:
{{ macros.mock_public_fields(config_values, 'infra') }}

  protected:
{{ macros.mock_raw_methods(config_values, 'infra') }}
};

} // namespace porytiles2

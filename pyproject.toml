[project]
name = "porytiles-config-gen"
version = "0.1.0"
description = "Configuration code generation for Porytiles2"
requires-python = ">=3.13"
dependencies = [
    "Jinja2>=3.1.6",
    "PyYAML>=6.0.3",
]

[tool.uv]
# This is a scripts-only project, not a Python package
package = false

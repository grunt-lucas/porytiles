#ifndef GUARD_METATILE_BEHAVIORS_H
#define GUARD_METATILE_BEHAVIORS_H

/* The block comment below never terminates, so the tolerant scans fail on lex and the scanner must record the header
   as unreadable rather than claiming it declares no MB_ name.
enum {
    MB_NORMAL,
    MB_TALL_GRASS,
};

#endif // GUARD_METATILE_BEHAVIORS_H

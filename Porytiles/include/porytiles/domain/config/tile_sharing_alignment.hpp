#pragma once

#include <algorithm>
#include <cctype>
#include <format>
#include <optional>
#include <ostream>
#include <string>

#include "porytiles/utilities/panic/panic.hpp"

/*
 * NOTE: DO NOT EDIT THIS FILE DIRECTLY. It is AUTO-GENERATED from config_schema.yaml.
 * To add new config values or make other changes, edit config_schema.yaml and regenerate via:
 *
 *   uv run Scripts/generate_config.py
 */

namespace porytiles {

/**
 * @brief Controls palette slot alignment strategy for tile sharing deduplication.
 *
 * @details
 * When set to off, palettes are filled sequentially with no sharing attempt.
 * When greedy, indirect links align palette slot indices for color-isomorphic
 * tiles. When optimal, a CSP-based solver finds globally optimal alignment (not yet implemented).
 * 
 */
enum class TileSharingAlignment {
    /**
     * @brief Pure sequential fill with no sharing alignment (default).
     */
    off,
    /**
     * @brief Best-effort alignment via indirect link resolution.
     */
    greedy,
    /**
     * @brief CSP-based globally optimal alignment (not yet implemented).
     */
    optimal
};

/**
 * @brief Parses a string into a TileSharingAlignment with fuzzy matching.
 *
 * @details
 * Performs parsing in two phases:
 * 1. Exact match against C++ constant names (snake_case)
 * 2. Case-insensitive fuzzy match against configured fuzzy_names
 *
 * This unified function is used by both CLI and YAML parsing. The fuzzy matching
 * allows users to use various common formats (kebab-case, snake_case, etc.) while
 * still getting good error messages through LazyLayeredConfig.
 *
 * @param str The string to parse
 * @return The parsed value, or std::nullopt if the string is invalid
 */
[[nodiscard]] inline std::optional<TileSharingAlignment> tile_sharing_alignment_from_str(const std::string &str)
{
    // Phase 1: Exact match against C++ constant names
    if (str == "off") {
        return std::optional{ TileSharingAlignment::off };
    }
    if (str == "greedy") {
        return std::optional{ TileSharingAlignment::greedy };
    }
    if (str == "optimal") {
        return std::optional{ TileSharingAlignment::optimal };
    }

    // Phase 2: Case-insensitive fuzzy match
    std::string lower_str = str;
    std::ranges::transform(
            lower_str, lower_str.begin(), [](unsigned char c) { return static_cast<char>(std::tolower(c)); });

    // Fuzzy names for off
    if (lower_str == "off") {
        return std::optional{ TileSharingAlignment::off };
    }
    // Fuzzy names for greedy
    if (lower_str == "greedy") {
        return std::optional{ TileSharingAlignment::greedy };
    }
    // Fuzzy names for optimal
    if (lower_str == "optimal") {
        return std::optional{ TileSharingAlignment::optimal };
    }

    return std::nullopt;
}

/**
 * @brief Converts a TileSharingAlignment to its canonical string representation.
 *
 * @details
 * Returns the C++ constant name (snake_case). This is the canonical form used for
 * general purpose string conversion, logging, and error messages.
 *
 * @param m The value to convert
 * @return The canonical snake_case string representation
 */
[[nodiscard]] inline std::string to_string(const TileSharingAlignment m)
{
    switch (m) {
    case TileSharingAlignment::off:
        return "off";
    case TileSharingAlignment::greedy:
        return "greedy";
    case TileSharingAlignment::optimal:
        return "optimal";
    }
    panic("unhandled TileSharingAlignment value");
}

/**
 * @brief Stream insertion operator for TileSharingAlignment.
 *
 * @param os The output stream
 * @param m The value to output
 * @return Reference to the output stream
 */
inline std::ostream &operator<<(std::ostream &os, const TileSharingAlignment m)
{
    return os << to_string(m);
}

} // namespace porytiles

template <>
struct std::formatter<porytiles::TileSharingAlignment> {
    constexpr auto parse(std::format_parse_context &ctx)
    {
        return ctx.begin();
    }

    auto format(const porytiles::TileSharingAlignment &value, auto &ctx) const
    {
        return std::format_to(ctx.out(), "{}", porytiles::to_string(value));
    }
};